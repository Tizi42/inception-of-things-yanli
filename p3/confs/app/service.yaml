apiVersion: v1
kind: Service
metadata:
  name: playground
spec:
  type: NodePort
  selector:
    app: playground
  ports:
    - name: http
      port: 8888
      targetPort: http
      nodePort: 30080

